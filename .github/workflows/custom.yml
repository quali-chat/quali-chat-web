name: Custom CI Workflow

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch: {}

jobs:
  dockerhub:
    uses: ./.github_custom/workflows/dockerhub.yaml

  prod-deploy:
    uses: ./.github_custom/workflows/prod_deploy.yaml
